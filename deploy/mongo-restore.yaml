apiVersion: v1
kind: Pod
metadata:
  name: mongo-restore-pod
  namespace: kalyanam
spec:
  serviceAccountName: gke-secret-accessor
  restartPolicy: Never
  volumes:
    - name: secret-volume
      emptyDir: {}
    - name: tmp-volume  # <-- Added for /tmp
      emptyDir: {}
    - name: aws-credentials  # <-- For S3 access
      secret:
        secretName: aws-s3-credentials  # Pre-created secret

  initContainers:
    - name: credential-loader
      image: gcr.io/google.com/cloudsdktool/cloud-sdk:latest
      volumeMounts:
        - name: secret-volume
          mountPath: /etc/secrets
        - name: aws-credentials  # <-- Mount AWS creds
          mountPath: /etc/aws
      command:
        - "/bin/sh"
        - "-c"
        - |
          # Load MongoDB password
          gcloud secrets versions access latest --secret=galadb_password > /etc/secrets/mongodb_password
          
          # Configure AWS CLI (if using S3)
          echo "[default]
          aws_access_key_id=$(cat /etc/aws/access-key)
          aws_secret_access_key=$(cat /etc/aws/secret-key)
          region=us-east-1" > /root/.aws/config

  containers:
    - name: restore-container
      image: gcr.io/google.com/cloudsdktool/cloud-sdk:latest
      volumeMounts:
        - name: secret-volume
          mountPath: /etc/secrets
          readOnly: true
        - name: tmp-volume  # <-- Proper /tmp mount
          mountPath: /tmp
        - name: aws-credentials
          mountPath: /etc/aws
      command:
        - "/bin/sh"
        - "-c"
        - |
          # Install tools
          apt-get update && apt-get install -y jq gnupg awscli
          
          # Install MongoDB Tools
          curl -fsSL https://pgp.mongodb.com/server-7.0.asc | \
            gpg --dearmor -o /usr/share/keyrings/mongodb-server-keyring.gpg
          echo "deb [signed-by=/usr/share/keyrings/mongodb-server-keyring.gpg] https://repo.mongodb.org/apt/debian bookworm/mongodb-org/7.0 main" | \
            tee /etc/apt/sources.list.d/mongodb-org-7.0.list
          apt-get update && apt-get install -y mongodb-database-tools

          # Get credentials
          MONGODB_PASSWORD=$(cat /etc/secrets/mongodb_password)
          MONGODB_URI="mongodb+srv://galaDbUser:$(echo ${MONGODB_PASSWORD} | jq -Rr @uri)@cluster0.sod5j.mongodb.net"

          # --- Choose either GCS or S3 method below ---

          # Option A: GCS Backup
          LATEST_BACKUP=$(gsutil ls -l gs://shravani_kalyanam_bucket/backups/mongo-backup-*.gz | sort -k 2 | tail -n 1 | awk '{print $3}')
          gsutil cp "${LATEST_BACKUP}" /tmp/latest_backup.gz

          # Option B: S3 Backup (uncomment to use)
          # LATEST_BACKUP=$(aws s3api list-objects-v2 --bucket your-s3-bucket --prefix backups/ \
          #   --query 'sort_by(Contents,&LastModified)[-1].Key' --output text)
          # aws s3 cp "s3://your-s3-bucket/${LATEST_BACKUP}" /tmp/latest_backup.gz

          # Restore to new DB
          NEW_DB_NAME="restored_db_$(date +"%Y%m%d_%H%M%S")"
          mongorestore --uri="${MONGODB_URI}" \
            --archive=/tmp/latest_backup.gz \
            --gzip \
            --nsFrom="*" \
            --nsTo="${NEW_DB_NAME}.*" \
            --drop

          # Verification
          echo "=== Restore Complete ==="
          mongosh "${MONGODB_URI}/${NEW_DB_NAME}" --eval "db.stats()"
